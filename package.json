{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint 'packages/**/*.ts{,x}'",
    "start": "yarn workspace slide start",
    "run-ink": "yarn workspace ink-demo start",
    "demo": "yarn workspace @koba04/react-fs start",
    "test": "yarn workspace @koba04/react-fs test"
  },
  "dependencies": {
    "react": "^16.10.2"
  },
  "devDependencies": {
    "@cybozu/eslint-config": "^7.1.0",
    "eslint": "^6.6.0",
    "prettier": "^1.18.2",
    "typescript": "^3.6.4"
  }
}
